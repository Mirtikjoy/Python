# using {} we create a set
# set are unorder 
# set can't have any duplicate elements if there is any duplicate python automatic detects and just remove the duplicate element
# as sets are not in order form they can't be inserted whereever we want
names = {'mirtik joy molsom', 90, 'my file', '25'}
print(names)

print(list(enumerate(names)))

